name: ProxiesAction

on:
  push:
    branches: [ "GitAction" ]
jobs:
    build:
      name: proxies-action
      runs-on: linux
      steps:
        - uses: actions/checkout@v2
        - name: login to docker hub
          id: docker-hub
          env:
            username: ${{secrets.DOCKER_USER}}
            password: ${{secrets.DOCKER_PASS0}}
          run: |
            docker login -u $username -p $password 
        - name: build the docker image
          id: build-docker-image
          run: |
            ls -la 
            docker build . -f Dockerfile -t mract1on/proxies-action-test:latest
        - name: push the docker image
          id: push-docker-image
          run: docker push ${{secrets.DOCKER_USER}}/proxies-action-test:latest
